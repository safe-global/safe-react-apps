import React from 'react';
declare type Props = {
    steps: Array<{
        id: string;
        label: string;
    }>;
    activeStepIndex: number;
    error?: boolean;
    orientation: 'vertical' | 'horizontal';
};
declare const Stepper: ({ steps, error, activeStepIndex, orientation, }: Props) => React.ReactElement;
export default Stepper;
