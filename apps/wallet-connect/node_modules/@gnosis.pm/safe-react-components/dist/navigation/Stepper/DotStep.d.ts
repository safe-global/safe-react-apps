import React from 'react';
declare type Props = {
    dotIndex: number;
    currentIndex: number;
    error?: boolean;
};
declare const DotStep: ({ currentIndex, dotIndex, error, }: Props) => React.ReactElement;
export default DotStep;
