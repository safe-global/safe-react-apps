import { ReactElement } from 'react';
import { IconType } from '../../dataDisplay/Icon';
export declare type Item = {
    id: string;
    icon?: keyof IconType;
    label: string;
    customContent?: ReactElement;
    disabled?: boolean;
};
declare type Variant = 'outlined' | 'contained';
export declare type Props = {
    onChange: (selectedIndex: string) => void;
    items: Array<Item>;
    selectedTab: string;
    variant?: Variant;
    fullWidth?: boolean;
};
declare const Tab: ({ onChange, items, selectedTab, variant, fullWidth, }: Props) => JSX.Element;
export default Tab;
