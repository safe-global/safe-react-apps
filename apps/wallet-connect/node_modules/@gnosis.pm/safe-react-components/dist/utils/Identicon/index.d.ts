import * as React from 'react';
import { ThemeIdenticonSize } from '../../theme';
declare type Props = {
    address: string;
    size: ThemeIdenticonSize;
};
declare const Identicon: ({ size, address, ...rest }: Props) => React.ReactElement;
export default Identicon;
