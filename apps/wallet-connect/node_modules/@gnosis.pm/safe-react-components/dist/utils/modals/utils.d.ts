import React from 'react';
declare type Props = {
    okText?: string;
    cancelText?: string;
    okDisabled?: boolean;
    handleOk: () => void;
    handleCancel: () => void;
};
export declare const ModalFooterConfirmation: ({ cancelText, handleCancel, okDisabled, handleOk, okText, }: Props) => React.ReactElement;
export {};
