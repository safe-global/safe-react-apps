import React from 'react';
import { EllipsisMenuItem } from '../../';
import { ThemeTextSize, ThemeColors, ThemeIdenticonSize } from '../../theme';
import { ExplorerInfo } from '../../typings/misc';
declare type Props = {
    hash: string;
    textColor?: ThemeColors;
    textSize?: ThemeTextSize;
    identiconSize?: ThemeIdenticonSize;
    shortenHash?: number;
    className?: string;
    name?: string;
    showIdenticon?: boolean;
    showCopyBtn?: boolean;
    menuItems?: EllipsisMenuItem[];
    explorerUrl?: ExplorerInfo;
};
declare const EthHashInfo: ({ hash, name, textColor, textSize, identiconSize, className, shortenHash, showIdenticon, showCopyBtn, menuItems, explorerUrl, }: Props) => React.ReactElement;
export default EthHashInfo;
