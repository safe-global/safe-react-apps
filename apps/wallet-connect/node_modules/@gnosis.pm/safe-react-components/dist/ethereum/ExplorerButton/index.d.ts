import React from 'react';
import { ExplorerInfo } from '../../typings/misc';
declare type Props = {
    className?: string;
    explorerUrl: ExplorerInfo;
};
declare const ExplorerButton: ({ className, explorerUrl, }: Props) => React.ReactElement;
export default ExplorerButton;
