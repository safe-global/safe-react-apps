import React from 'react';
declare type Props = {
    className?: string;
    orientation?: 'vertical' | 'horizontal';
};
declare const Divider: ({ className, orientation }: Props) => React.ReactElement;
export default Divider;
