import React from 'react';
declare type Props = {
    children: React.ReactNode;
};
/**
 * Use `Section` to highlight/group content.
 */
declare const Section: ({ children }: Props) => React.ReactElement;
export default Section;
