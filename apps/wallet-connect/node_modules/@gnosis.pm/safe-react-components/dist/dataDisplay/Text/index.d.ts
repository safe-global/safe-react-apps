import React from 'react';
import { ThemeTextSize, ThemeColors } from '../../theme';
declare type Props = {
    children: React.ReactNode;
    size: ThemeTextSize;
    color?: ThemeColors;
    strong?: boolean;
    center?: boolean;
    tooltip?: string;
    className?: string;
    as?: 'span' | 'p';
};
declare const Text: ({ children, as, tooltip, ...rest }: Props) => React.ReactElement;
export default Text;
