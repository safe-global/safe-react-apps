import React from 'react';
declare type Props = {
    title: string;
    body: React.ReactNode;
    onCancel: () => void;
    onConfirm: () => void;
};
declare const FixedDialog: ({ body, title, onConfirm, onCancel, }: Props) => React.ReactElement;
export default FixedDialog;
