import React from 'react';
declare type Props = {
    checked: boolean;
    onChange: (checked: boolean) => void;
};
declare const Switch: ({ checked, onChange }: Props) => React.ReactElement;
export default Switch;
