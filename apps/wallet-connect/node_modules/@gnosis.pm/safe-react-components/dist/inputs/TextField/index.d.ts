import React from 'react';
declare type Props = {
    value: string;
    label: string;
    readOnly?: boolean;
    meta?: {
        error?: string;
    };
    input?: React.InputHTMLAttributes<HTMLInputElement>;
    startAdornment?: React.ReactElement;
    endAdornment?: React.ReactElement;
    className?: string;
} & React.InputHTMLAttributes<HTMLInputElement>;
declare function TextField({ input, value, onChange, meta, readOnly, label, startAdornment, endAdornment, className, ...rest }: Props): React.ReactElement;
export default TextField;
