import React from 'react';
export declare type SelectItem = {
    id: string;
    label: string;
    subLabel?: string;
    iconUrl?: string;
};
declare type Props = {
    items: Array<SelectItem>;
    activeItemId: string;
    onItemClick: (id: string) => void;
    id?: string;
    fallbackImage?: string;
};
declare function Select({ items, activeItemId, onItemClick, id, fallbackImage, ...rest }: Props): React.ReactElement;
export default Select;
