import React from 'react';
export interface Props {
    label: string;
    checked: boolean;
    name: string;
    onChange: (event: React.ChangeEvent<HTMLInputElement>, checked: boolean) => void;
    meta?: any;
    input?: any;
}
declare const Checkbox: ({ checked, label, onChange, meta, input, ...rest }: Props) => React.ReactElement;
export default Checkbox;
