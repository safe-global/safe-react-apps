import React from 'react';
import { ThemeLoaderSize, ThemeColors } from '../../theme';
declare type Props = {
    size: ThemeLoaderSize;
    color?: ThemeColors;
    className?: string;
};
declare const Loader: ({ className, size, color }: Props) => React.ReactElement;
export default Loader;
