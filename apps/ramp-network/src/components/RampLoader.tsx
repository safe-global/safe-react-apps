import { Loader, Title } from '@gnosis.pm/safe-react-components';

const RampLoader = () => (
  <>
    <Title size="md">Waiting for Safe...</Title>
    <Loader size="md" />
  </>
);

export default RampLoader;
