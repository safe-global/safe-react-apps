{
  "name": "safe-apps",
  "version": "1.6.2",
  "private": true,
  "scripts": {
    "start-compound": "cd compound && react-app-rewired start",
    "build-counpound": "cd compound && react-app-rewired build",
    "ipfs-deploy-compound": "cd compound && yarn build && npx ipfs-deploy build --pinner pinata --no-open"
  },
  "workspaces": [
    "compound",
    "tx-builder",
    "wallet-connect"
  ]
}
