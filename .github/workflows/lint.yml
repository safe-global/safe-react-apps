name: 'ESLint check'
on: [pull_request]
env:
  HUSKY: 0

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gnosis/safe-react-eslint-plus-action@main
